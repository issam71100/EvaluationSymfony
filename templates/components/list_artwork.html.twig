<section class="shop-landing">
	<div class="container">
		<h2 class="title">{{ title }}</h2>

		{% if categories | length > 0 %}
			{% include 'components/filters.html.twig' with {'categories': categories} only%}
		{% endif %}


		<div class="row">
			{% set i = 0%}
			{% for artwork in artworks %}
				<div class="col-md-4 col-sm-6 col-xs-12 mb-5">
					{% include 'components/card.html.twig' %}
				</div>
			{% endfor %}
		</div>
	</div>
</section>
