<section class="shop-landing">
	<div class="container">
		<h2 class="title">{{ title }}</h2>
		<div class="row">
			{% set i = 0%}
			{% for exposition in expositions %}
				<div class="col-md-4 col-sm-6 col-xs-12 mb-5">
					{% include 'components/card_expo.html.twig' %}
				</div>
			{% endfor %}
		</div>
	</div>
</section>
