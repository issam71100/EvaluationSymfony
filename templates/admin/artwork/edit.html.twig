{% extends 'base.html.twig' %}

{% block title %}Edit Artwork{% endblock %}

{% block header %}
	<section class="header-image" style="background-image : url('{{ asset('uploads/img/artworks/' ~ artwork.image )}}');">
		<div class="book-text">
			<h1>{{sitetitle}}</h1>
		</div>
	</section>
{% endblock %}

{% block body %}
    <h1>Edit Artwork</h1>

    {{ include('admin/artwork/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('admin.artwork.index') }}">back to list</a>

    {{ include('admin/artwork/_delete_form.html.twig') }}
{% endblock %}
